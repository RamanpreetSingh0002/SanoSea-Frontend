import React from "react";

const AllBooking = ({ children }) => {
  return (
    <section id="all-booking">
      <div class="all-booking-wrapper">{children}</div>
    </section>
  );
};

export default AllBooking;
